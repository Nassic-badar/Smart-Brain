import React from 'react';

const Rank = ({ name, entries }) => {
  return (
    <div>
      <div className='white f3'>
        Clarifai server are down sometimes , you have try later
      </div>
    </div>
  );
}

export default Rank;